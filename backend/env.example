# Jarvis UI Backend Configuration
# Rename this file to .env and update with your values

# ============================================
# DATABASE SETTINGS
# ============================================

# PostgreSQL with asyncpg driver
# For remote server: use kakischer.tplinkdns.com:20004
DATABASE_URL=postgresql+asyncpg://USERNAME:PASSWORD@kakischer.tplinkdns.com:20004/jarvis

# Memory database (optional - defaults to main DATABASE_URL)
# Uses the 'memory' database for long-term memory storage
MEMORY_DATABASE_URL=postgresql+asyncpg://USERNAME:PASSWORD@kakischer.tplinkdns.com:20004/memory

# ============================================
# LLM PROVIDER SETTINGS
# ============================================

# LLM Provider: openai, anthropic, gemini, n8n, mock
LLM_PROVIDER=openai

# Model name (provider-specific)
# OpenAI (recommended): gpt-4o-mini (best balance), gpt-4o (most capable), gpt-4-turbo
# OpenAI (legacy): gpt-5-nano, gpt-3.5-turbo
# Anthropic: claude-3-5-sonnet-20241022, claude-3-opus-20240229
# Gemini: gemini-1.5-pro, gemini-1.5-flash, gemini-pro
LLM_MODEL=gpt-4o-mini

# OpenAI API Key (required if LLM_PROVIDER=openai)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Anthropic API Key (required if LLM_PROVIDER=anthropic)
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# Google Gemini API Key (required if LLM_PROVIDER=gemini)
# GEMINI_API_KEY=your-gemini-api-key-here

# ============================================
# N8N SETTINGS
# ============================================

# OPTION 1: Direct API calls (recommended - more reliable)
# n8n API URL (base URL for n8n API)
N8N_API_URL=http://kakischer.tplinkdns.com:20003/api/v1

# n8n API Key (generate in n8n: Settings > API > Generate API Key)
N8N_API_KEY=your-n8n-api-key-here

# OPTION 2: Tool Executor webhook (alternative)
# n8n Tool Executor webhook URL (for infrastructure tools)
# Only used if N8N_API_URL and N8N_API_KEY are not set
N8N_TOOL_EXECUTOR_URL=http://kakischer.tplinkdns.com:20003/webhook/tool-executor

# Timeout for n8n tool execution (seconds)
N8N_TIMEOUT_SECONDS=120

# Legacy n8n webhook (for backwards compatibility/fallback)
# Only used if LLM_PROVIDER=n8n
N8N_WEBHOOK_URL=http://kakischer.tplinkdns.com:20003/webhook/jarvis

# ============================================
# SERVER SETTINGS
# ============================================

# Server - 0.0.0.0 allows connections from any IP
HOST=0.0.0.0
PORT=20005

# CORS - Allow all origins (use specific domains for production)
CORS_ORIGINS=*

# Enable streaming responses
STREAM_ENABLED=true

# SSL verification (set to false if behind corporate proxy with self-signed certs)
VERIFY_SSL=true

# ============================================
# SESSION SETTINGS
# ============================================

# Days before inactive sessions expire
SESSION_TTL_DAYS=30
