# Jarvis UI Backend Configuration
# Rename this file to .env and update with your values

# ============================================
# DATABASE SETTINGS
# ============================================

# PostgreSQL with asyncpg driver
# For remote server: use kakischer.tplinkdns.com:20004
DATABASE_URL=postgresql+asyncpg://n8n:n8npass@kakischer.tplinkdns.com:20004/jarvis

# Memory database (optional - defaults to main DATABASE_URL)
# Uses the 'memory' database for long-term memory storage
MEMORY_DATABASE_URL=postgresql+asyncpg://n8n:n8npass@kakischer.tplinkdns.com:20004/memory

# ============================================
# LLM PROVIDER SETTINGS
# ============================================

# LLM Provider: openai, anthropic, gemini, n8n, mock
LLM_PROVIDER=openai

# Model name (provider-specific)
# OpenAI: gpt-5-nano, gpt-4o, gpt-4-turbo, gpt-3.5-turbo
# Anthropic: claude-3-opus-20240229, claude-3-sonnet-20240229
# Gemini: gemini-1.5-pro, gemini-1.5-flash, gemini-pro
LLM_MODEL=gpt-5-nano

# OpenAI API Key (required if LLM_PROVIDER=openai)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Anthropic API Key (required if LLM_PROVIDER=anthropic)
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# Google Gemini API Key (required if LLM_PROVIDER=gemini)
# GEMINI_API_KEY=your-gemini-api-key-here

# ============================================
# N8N TOOL EXECUTOR SETTINGS
# ============================================

# n8n Tool Executor webhook URL (for infrastructure tools)
# This is the single entry point for all n8n tool calls
N8N_TOOL_EXECUTOR_URL=http://kakischer.tplinkdns.com:20002/webhook/tool-executor

# Timeout for n8n tool execution (seconds)
N8N_TIMEOUT_SECONDS=120

# Legacy n8n webhook (for backwards compatibility/fallback)
# Only used if LLM_PROVIDER=n8n
N8N_WEBHOOK_URL=http://kakischer.tplinkdns.com:20002/webhook/jarvis

# ============================================
# SERVER SETTINGS
# ============================================

# Server - 0.0.0.0 allows connections from any IP
HOST=0.0.0.0
PORT=20005

# CORS - Allow all origins (use specific domains for production)
CORS_ORIGINS=*

# Enable streaming responses
STREAM_ENABLED=true

# ============================================
# SESSION SETTINGS
# ============================================

# Days before inactive sessions expire
SESSION_TTL_DAYS=30
